// Map of local asset filenames to require(...) calls.
// Add your local images (placed under mobile/assets) here so components can use
// the convenient `assets/<filename>` string as the image `source`.

// Example:
// export const assetsMap: Record<string, any> = {
//   'chicken.png': require('../../assets/chicken.png'),
//   'burger.png': require('../../assets/burger.png'),
// };

export const assetsMap: Record<string, any> = {};

export default assetsMap;
